# ---- Glass Bridge client environment ----
# Used only by the Vite dev server. In production the client is served by the
# backend on the same origin, so no API URL is needed.

# Where the dev server proxies /api and /socket.io (the backend).
VITE_API_PROXY=http://localhost:4000
